# Inherited Dependabot workflow. Disabled; this fork is not using mango CI.
name: Dependabot PR Review

on:
  workflow_dispatch:

jobs:
  skipped:
    name: Skipped on Nova fork
    runs-on: ubuntu-latest
    steps:
      - run: echo "Dependabot CI is disabled."
