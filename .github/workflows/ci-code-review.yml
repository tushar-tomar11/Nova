# Inherited from mango-v4-ui. Disabled on this fork: Trivy fails on
# upstream CRITICAL CVEs, and lint-all fails on existing lint debt.
# Vercel is the deploy gate. Re-enable via workflow_dispatch if needed.
name: Code Review

on:
  workflow_dispatch:

jobs:
  skipped:
    name: Skipped on Nova fork
    runs-on: ubuntu-latest
    steps:
      - run: echo "Code Review is disabled; deploy via Vercel."
